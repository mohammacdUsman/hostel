<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Cinzel:wght@700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            margin: 0; height: 100vh; display: flex; justify-content: center; align-items: center;
            background: linear-gradient(rgba(0,0,0,0.8), rgba(0,0,0,0.8)), url('https://images.unsplash.com/photo-1555854877-bab0e564b8d5?q=80&w=1920');
            background-size: cover; font-family: 'Poppins', sans-serif;
        }
        .neo {
            width: 400px; padding: 40px; background: #2b2b2b; border-radius: 25px;
            box-shadow: 15px 15px 30px rgba(0,0,0,0.6), -10px -10px 30px rgba(255,255,255,0.05);
            border: 1px solid rgba(212, 175, 55, 0.1); color: white;
        }
        .neo input {
            width: 100%; padding: 15px; border-radius: 16px; border: none; background: #2b2b2b; color: #ddd;
            box-shadow: inset 5px 5px 10px rgba(0,0,0,0.5), inset -5px -5px 10px rgba(255,255,255,0.05); margin-bottom: 20px;
        }
        .neo-btn {
            width: 100%; padding: 15px; border-radius: 16px; background: linear-gradient(145deg, #D4AF37, #b5952f);
            border: none; font-weight: 700; color: #1a1a1a; cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="neo">
        <h3 class="text-center mb-2" style="font-family: 'Cinzel'; color: #D4AF37;">Welcome Back</h3>
        
        <!-- EJS Error Handling -->
        <% if (error) { %>
            <div class="alert alert-danger py-2 mb-4 small text-center rounded-pill"><%= error %></div>
        <% } %>

        <form method="POST" action="/login">
            <input type="email" name="email" placeholder="Email Address" required>
            <input type="password" name="password" placeholder="Password" required>
            <button class="neo-btn">LOGIN</button>
        </form>

        <a href="/register" style="display: block; text-align: center; margin-top: 25px; text-decoration: none; color: #aaa;">
            Don't have an account? <span style="color: #D4AF37;">Sign Up</span>
        </a>
    </div>
</body>
</html>